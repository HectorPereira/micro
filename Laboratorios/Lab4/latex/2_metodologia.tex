\section{Metodología}

\subsection{Materiales y Herramientas}

\begin{itemize}
    \item \textbf{Microcontrolador principal:}
    \begin{itemize}
        \item ATmega328P
    \end{itemize}

    \item \textbf{Módulos y periféricos electrónicos:}
    \begin{itemize}
        \item Sensor de temperatura LM35
        \item Ventilador DC 5~V
        \item Motor DC + potenciómetro acoplado
        \item Joystick analógico
        \item Botón pulsador
        \item Matriz de LEDs WS2812 (8×8)
        \item Módulo RFID MFRC522
        \item Pantalla LCD 16×2 con interfaz I2C (PCF8574)
        \item Memoria EEPROM interna
        \item LEDs indicadores (rojo, verde)
        \item Buzzer piezoeléctrico
        \item Fuente de alimentación 5~V–12~V
    \end{itemize}

    \item \textbf{Herramientas de programación y simulación:}
    \begin{itemize}
        \item Microchip Studio (Atmel Studio 7)
        \item Proteus 8 / PicSimLab
        \item Python 3.x
        \item Bibliotecas Python: matplotlib, threading, serial (pySerial), collections.deque
    \end{itemize}
\end{itemize}



\subsection{Procedimiento general}

\subsection{Maestro Esclavo - SPI}
Para este codigo, se empleo una libreria con todas las funciones e ISR's utilizadas, para poder ver el main con claridad.


\subsubsection{Inicialización SPI}

Se implementa la comunicación maestro–esclavo entre dos microcontroladores ATmega328P utilizando el protocolo SPI. Para ello se emplean las funciones de inicialización correspondientes tanto al maestro como al esclavo, mostradas en \ref{anexo:MasterSPIIn} y \ref{anexo:SlaveSPIIn}. La configuración seleccionada utiliza una frecuencia de reloj de 8~MHz, según los valores permitidos por el módulo SPI del ATmega328P, como se aprecia en la tabla de la figura \ref{fig:HZ}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{anexos/1 SPI/Frecuencia.png}
    \caption{Tabla de frecuencia SPI \url{https://ww1.microchip.com/downloads/en/DeviceDoc/Atmel-7810-Automotive-Microcontrollers-ATmega328P_Datasheet.pdf}}
    \label{fig:HZ}
\end{figure}

\subsubsection{Transferencia SPI}

La transferencia de datos desde el maestro hacia el esclavo se realiza mediante la función presentada en \ref{anexo:Transfer_spi}, la cual espera a que la bandera \texttt{SPIF} (SPI Interrupt Flag) se establezca. Esta bandera indica que la transmisión ha finalizado y que el dato se encuentra disponible en el registro \texttt{SPDR} (SPI Data Register). 

La recepción en el esclavo se efectúa mediante la función mostrada en \ref{anexo:Reception_spi}, que opera de forma similar: espera a la finalización del intercambio de bits y posteriormente retorna el byte recibido desde el maestro a través del bus SPI.

\subsubsection{Fotoresistor}

Para la lectura del fotoresistor se utiliza el fragmento de código indicado en \ref{anexo:fotoresistor}. En este esquema, el sensor está conectado en conjunto con una resistencia de tipo pull-up, y el valor analógico obtenido permite estimar la intensidad lumínica. La variación de resistencia del LDR modifica la caída de tensión en el divisor, lo que posibilita evaluar el nivel de luz ambiente mediante el ADC del ATmega328P.

\subsubsection{Potenciómetro – SG90}

Para la lectura del potenciómetro se utiliza el código mostrado en \ref{anexo:Potenciometro}, donde el valor obtenido mediante el ADC es convertido a un ángulo en el rango de 0 a 180 grados. Esta conversión se realiza aplicando la relación lineal \(180/1023\), correspondiente a la resolución del conversor analógico-digital del ATmega328P. Posteriormente, el valor numérico es transformado a su representación ASCII mediante la función \texttt{add\_string}, permitiendo su envío al esclavo para el control del servo SG90.

\subsubsection{Lectura del HC-SR04 y transferencia de distancia}

Para la medición de distancia con el sensor ultrasónico \textbf{HC-SR04} se emplea el procedimiento indicado en \ref{anexo:hc-sr04}, basado en la detección del pulso de \textit{echo} mediante un contador por comparación. El método consiste en medir la cantidad de \textit{ticks} que la señal permanece en nivel alto y convertir dicho tiempo en distancia utilizando la ecuación correspondiente del sensor. 

En función del valor calculado, el sistema asigna un código distinto que es transferido al microcontrolador esclavo, permitiendo ejecutar acciones diferenciadas según la distancia detectada por el HC-SR04.


\subsubsection{Lectura del DHT11 y transferencia de temperatura}

La lectura del DHT11 se realiza mediante su protocolo de comunicación específico, utilizando la función mostrada en \ref{anexo:DHT11}. En este proceso se emplean punteros para actualizar variables globales con los valores de temperatura y humedad obtenidos del sensor. Tal como se observa en \ref{anexo:DHT11T}, la función retorna un valor de tipo \texttt{bool} que indica si la lectura fue válida, permitiendo ejecutar la evaluación correspondiente sin necesidad de procesamientos adicionales.

Una vez adquirida la medición, el sistema compara la temperatura registrada con un valor de referencia (temperatura ambiente). Según el resultado de esta comparación, se envía un código al microcontrolador esclavo indicando si debe activar o no el ventilador asociado. Esto permite implementar una lógica básica de control térmico distribuido mediante comunicación SPI.

\subsubsection{Escritura en LCD}

Para la visualización de los datos en la pantalla LCD se emplean dos búferes, \texttt{L1} y \texttt{L2}, en los cuales se concatenan los valores obtenidos durante la ejecución mediante la función \texttt{strcat}. De esta forma, cada cadena se construye dinámicamente con la información correspondiente y luego es enviada al módulo LCD para su actualización en pantalla. L1 para la primer linea y L2 para la segunda.


\subsubsection{Recepcion de datos en esclavo}

Para ver el main del esclavo, vease ~\ref{anexo:RE}. Se implemento la recepcion mediante una unica funcion SPI slaveReceive, y luego se evalua con un switch el valor del mismo. Teniendo reservado valores para cada accion a realizar. Las cuales se pueden ver en la tabla \ref{tab:tabspi} y \ref{tab:tabspi2}.

\begin{table}[h]
    \centering
    \caption{\textbf{TABLE I.} \textit{Comandos de Control de Salidas (GPIO y RGB)}}
    \label{tab:comandos_spi_salidas_estrecha}
    \begin{tabular}{c|p{4.5cm}} % Eliminadas las barras verticales excepto en la división de encabezado
        \hline
        \textbf{Comando (Hex/Dec)} & \textbf{Acción y Pines Afectados} \\
        \hline
        \texttt{0xB6} / 182 & \textbf{Activa} \texttt{PORTD2}. \\
        \texttt{0xB7} / 183 & \textbf{Desactiva} \texttt{PORTD2}. \\
        \hline
        \texttt{0xB9} / 185 & \textbf{Rojo} (\texttt{PD3}=1). \\
        \texttt{0xBA} / 186 & \textbf{Naranja} (\texttt{PD3}=1, \texttt{PD4}=1). \\
        \texttt{0xBB} / 187 & \textbf{Amarillo} (\texttt{PD3}=1, \texttt{PD4}=1). \\
        \texttt{0xBC} / 188 & \textbf{Verde} (\texttt{PD4}=1). \\
        \texttt{0xBD} / 189 & \textbf{Celeste} (\texttt{PD4}=1, \texttt{PD5}=1). \\
        \texttt{0xBF} / 191 & \textbf{Azul} (\texttt{PD5}=1). \\
        \hline
    \end{tabular}
    \label{tab:tabspi}
\end{table}


\begin{table}[h]
    \centering
    \caption{\textbf{TABLE II.} \textit{Comandos de Control de Tiempos (PWM y Servo)}}
    \label{tab:comandos_spi_tiempos_estrecha}
    \begin{tabular}{c|p{4.5cm}} % Eliminadas las barras verticales excepto en la división de encabezado
        \hline
        \textbf{Comando (Hex/Dec)} & \textbf{Acción y Registros Afectados} \\
        \hline
        \texttt{0xF0} / 240 & PWM (\texttt{OCR0A}): \textbf{Máximo (250)}. \\
        \texttt{0xB5} / 181 & PWM (\texttt{OCR0A}): \textbf{Apagado (0)}. \\
        \hline
        \textbf{\texttt{0x00}-\texttt{0xB4} / 0-180} & Posición del \textbf{Servo} ($0^\circ$ a $180^\circ$). Mapeado a \texttt{OCR1A}. \\
        \hline
    \end{tabular}
    \label{tab:tabspi2}
\end{table}



\subsection{Maestro Esclavo - I2c}

